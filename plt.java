import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class plt here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class plt extends Actor
{
    /**
     * Act - do whatever the plt wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public plt()
       {
           GreenfootImage image = getImage();
           image.scale(60,60);
           setImage(image);
        }
    public void act() 
    {
    }    
}
